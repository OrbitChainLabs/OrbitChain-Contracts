# Dependencies
/target

# Environment
.env
.env.local
.env.testnet
.env.mainnet

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Build artifacts
*.wasm
*.d

# Soroban
.stellar/
soroban.toml

# Deployment records (contain network-specific contract IDs; committed per-env as needed)
deployments/*.json
.orbitchain_contract_id

# Test artifacts
/signing_requests
/signed_transactions
/tmp
*.log
*.enc

# Test coverage
tarpaulin-report.html
cobertura.xml
tarpaulin-ci/

# JS tooling (wallet_connect.js dev/test deps — see package.json)
/node_modules
/playwright-report
/test-results
/blob-report

# Soroban test observation artifacts — regenerated on every test run;
# event-shape assertions live in insta snapshots instead (issue #115).
campaign/test_snapshots/
